[phases.setup]
cmds = ["npm install"]

[start]
cmd = "npm start"

[build]
base = "node"

[build.args]
NODE_ENV = "production"

[build.install]
cmds = ["npm install"]

[build.build]
cmds = ["npm run build"]
